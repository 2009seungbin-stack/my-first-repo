/** Honest comparisons from the head-to-head data in the repo docs; where the other tool is better is said.
 * Shape and rules: src/game-seo-families.js. Every claim rests on docs/STUDIO-*.md and the labels in src/game-seo.js. */
const page=(intent,ws,shot,copy,extra={})=>Object.freeze({intent,ws,shot,family:'compare',copy,...extra});
export const PAGES=Object.freeze({
});
