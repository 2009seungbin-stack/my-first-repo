/** Problem pages from real forum pain points: the cause, the fix and the tool that does it.
 * Shape and rules: src/game-seo-families.js. Every claim rests on docs/STUDIO-*.md and the labels in src/game-seo.js. */
const page=(intent,ws,shot,copy,extra={})=>Object.freeze({intent,ws,shot,family:'fixes',copy,...extra});
export const PAGES=Object.freeze({
});
